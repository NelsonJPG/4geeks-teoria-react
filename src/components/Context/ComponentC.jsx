import React from 'react'

const ComponentC = (props) => {
    return (
        <h2 style={{backgroundColor: 'red'}}>
            Componente C {props.nombre}
        </h2>
    )
}

export default ComponentC
